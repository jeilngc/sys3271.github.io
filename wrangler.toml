name = "sys-alliance"
compatibility_date = "2024-09-01"
pages_build_output_dir = "."

[[d1_databases]]
binding = "sys_alliance_db_test"                      # accessible in Functions as env.DB
database_name = "sys-alliance-db-test"
database_id = "c01bbfdb-1c81-465b-b355-be13634ac0ec"   # from `wrangler d1 create`

# Secrets (set these with `wrangler pages secret put NAME`, NOT here):
#   ADMIN_PASSWORD   - the shared dashboard password
#   SESSION_SECRET    - random long string used to sign session cookies
